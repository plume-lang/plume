native ("native", "js") "std:native" {
  ffi_print(x: str): int
  ffi_println(x: str): int
}

fn print<A extends show>(xs: ..A): unit {
  xs.map(fn (x) {
    ffi_print(x.show())
    ffi_print(" ")
  })

  return unit
}

fn println<A extends show>(xs: ..A): unit {
  xs.map(fn (x) {
    ffi_print(x.show())
    ffi_print(" ")
  })

  ffi_print("\n")
  return unit
}