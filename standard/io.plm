native "std:native.dylib" print(x: str): int
native "std:native.dylib" println<A>(x: A): int
native "std:native.dylib" get_args(): [str]
native "std:native.dylib" does_file_exist(path: str): bool
native "std:native.dylib" execute_command(path: str): str

@print(x) =>
  print(x.show())

@println(x) =>
  @print(x)
  print("\n")

@print_with(fmt, x) =>
  print(x.show(fmt))

@println_with(fmt, x) =>
  @print_with(fmt, x)
  print("\n")
