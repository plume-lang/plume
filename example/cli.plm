err: Result<str, str> = Ok("Hello, ")
x: Result<str, str> = err.map((s) => s.concat("world!"))
y = x.isOk()
println(x.show(show, show))
println(y.show())

x = 5.exp()
println(x.show())

facto(n) => product(1.slice(n))

ls: [int] = [0, 1, 2]
println(ls.show(show))
@println(facto(5))
println(ls.flatMap((x) => [x, x + 1, x + 2]).show(show))
println(ls[1..].show(show))
