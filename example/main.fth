x: int = 5

welcome(name: str) -> print("tet" + 5.3, 4)

add = (y: int) ->
    z: int = x + y + 45

welcome("Thomas")

factorial(n: int) ->
    if n == 0 then
        1
    else
        n * factorial(n - 1) + true

5 % x except n

t = x.n except r
in 2

print(factorial(5)) // rest
