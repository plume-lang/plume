x: int = 5

welcome(name: str) -> print("tet" + 5.3)

add = (y: int) ->
    z: int = x + y

welcome("Thomas")

factorial(n: int) ->
    if n == 0 then
        1
    else
        n * factorial(n - 1)

5 % x.n \ r

t = x.n \ r

print(factorial(5)) // rest
