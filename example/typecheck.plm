native print<A>(x: A): ()

property<A> A.show(): ()

extend<A extends show> (c: [A]) with
  show(): () => print(c)

extend (c: int) with
  show(): () => print(c)

test<A extends show>(x: [A]): () => x.show()

[[1, 2]].show()