DB.connect("tet") ->
    print("test")

facto(n) -> if n == 0 then 1 else n * facto(n - 1)

test = {
    x: 1,
    ...r
}

test = { x: 1, ...r }

test: { x: int, y: int, ...r } = { x: 1, ...r, y: 2 }
