@println(3 ^ 3)

println([1, 2, 3, 4, 5].show())

@println("Hello, World!")

@println(Some(8))

extend str {
  fn test(): str => "tesdt"
}

fn bruh(n: str) {
  [n, n, n, n].map(println)

  return n
}

@println(bruh("test"))

@println("Hello" + "World")

@println([1, 2, 3] == [4, 5, 6])
@println([1, 2, 3] == [1, 2, 3])
@println([1, 2, 3] != [4, 5, 6])
@println([1, 2, 3] != [1, 2, 3])

@println([1, 2, 3] + [4, 5, 6])
